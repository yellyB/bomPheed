import { HashRouter as Router, Route, Routes } from "react-router-dom";

const AppRouter = () => {
  return (
    <Router>
      <Routes>
        <Route />
      </Routes>
    </Router>
  );
};

export default AppRouter;
